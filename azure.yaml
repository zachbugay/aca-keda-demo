name: aca-keda-demo
metadata:
  template: aca-keda-demo@0.0.1
services:
  api:
    project: ./src/Api
    language: dotnet
    host: containerapp
    docker:
      image: aca-keda-demo/api-dev
      path: ./Dockerfile
      context: .
      remoteBuild: true
infra:
  provider: terraform
